import React from 'react';

import { Chef, Gallery, Header, SpecialMenu } from '../../container';

const Home = () => {
  return (
    <div>
        <Header />
        <Chef />
        <Gallery />
        <SpecialMenu />
    </div>
)};

export default Home;
